import 'package:test/test.dart';

void main() {
  test('package loads (scaffolding smoke test)', () {
    // Placeholder until real public API lands in later iterations.
    expect(true, isTrue);
  });
}
