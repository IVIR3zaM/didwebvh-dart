# very_good_analysis is the curated rule set replacing Checkstyle + SpotBugs
# from the Java project (see docs/PORTING-DECISIONS.md §2, §5).
include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - reference/**
    - "**/*.g.dart"
